import 'package:anime_app/models/dataconverter_data_model.dart';

class SeriesDetails {
  String? imageurl;
  // set setImageUrl(String? url) => _imageurl;
  // get getImageUrl => _imageurl ?? '';
  DataConverter? data;
  SeriesDetails({required this.data});
}
